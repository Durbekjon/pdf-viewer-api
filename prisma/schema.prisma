
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Pdf {
  id String @id @default(uuid())
  language Language @default(uz)
  file String
  @@map("pdfs")
}


enum Language {
  uz
  en
  ru
}